日志8.22
完成了：
模板的基本填充；
X和Y的统一读取；
原有功能的迁移和实现。
需说明的有：
涉及到多个参量在pathwiselearning()和coordinateDescent()之间的传递(如naive里的betaVector和residualVector)，暂时决定把两个合到一起，写两个pathwiselearning()；
神奇的是新的一套方法和旧方法运算式相同，但新方法误差更小，可能是运算时使用了不同函数的缘故。
接下来的目标：
测试集的读取与运算；
写一个新的结构体，以运算出的回归系数betaVector为核心，实现对任意X的预测；
画出图像（有可能）。
